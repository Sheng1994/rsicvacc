# Bare-metal software

This directory contains the RV32IM_Zicsr bare-metal runtime, directed tests,
and INT8 benchmarks. Programs are linked for the simulation memory map and
converted from ELF/binary into `$readmemh` images by the build system.
