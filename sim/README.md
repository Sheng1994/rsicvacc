# Simulation

This directory will contain Verilator/cocotb testbenches, memory models, trace
support, and FST waveform generation. `tests/` currently contains only a
host-side repository skeleton smoke test.
